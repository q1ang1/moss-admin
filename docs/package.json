{
  "name": "moss-docs",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm exec vitepress dev",
    "build": "pnpm exec vitepress build",
    "preview": "pnpm exec vitepress preview",
    "lint": "pnpm exec eslint .vitepress --ext .ts",
    "typecheck": "pnpm exec tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "eslint": "catalog:",
    "typescript": "catalog:",
    "vitepress": "catalog:"
  }
}
